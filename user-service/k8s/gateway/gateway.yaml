apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: salon-gateway
  namespace: salon
  annotations:
    alb.networking.azure.io/alb-name: salon-alb
    alb.networking.azure.io/alb-namespace: alb
spec:
  gatewayClassName: azure-alb-external
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same